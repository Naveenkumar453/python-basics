#function is block of code which is executed only when it is called.
# it reduces repitative task. Also called as invoking function
#the "Naveen" inside hello function invoke is arugument and the 'name ' is parameter in def
# there should be matching number of parameters for no.of arguments we pass
def hello(first_name, last_name, age):
    print("Hello! " + first_name + " " + last_name)
    print("you are "+ str(age) + " year's old")
    print("Have a nice day")
#my_name = "Naveen"
#hello(my_name)
hello("Naveen", "Kumar", 33)
# hello()
# hello()
